User wants ALL models (including judge model) to use minimax, not other providers. When I can't find a path user references, ask for exact path immediately instead of searching extensively.
§
User: all models including judge model must use minimax. When user references a path I can't find, ask for exact path immediately.