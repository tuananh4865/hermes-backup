# Hermes Daily Review — 2026-05-08

## ✅ Hoàn thành

### Wiki Repo Fix
- `my-llm-wiki` remote restored (was pointing to `hermes-backup`)
- Force push to resolve diverged history (607 vs 1874 commits)
- Cleaned up tracked files (unstaged `../skills/`, `../workers/`, `../.hermes/`)
- Updated `.gitignore` with proper entries
- **Flattened repo** — moved `wiki/` contents to repo ROOT (not inside `wiki/` folder)
- Final push: 4351 files changed

### Hermes Backup Cron Refactor
- Removed wiki from 3AM backup cron
- Skills copied from `/Volumes/Storage-1/Hermes/skills/` → `~/.hermes/skills/` (55 skills)
- Removed symlink (no longer needed with daily backup)
- Cron 3AM now backs up full `~/.hermes` → `hermes-backup` repo
- Wiki syncs via Obsidian plugin (every minute) → `my-llm-wiki`

### Memory System Test
- Discovered 2 WikiMemoryProvider files: stub (136 lines) vs full (1458 lines)
- Stub was being loaded instead of full implementation
- Fixed by replacing stub with full implementation
- All 3 hooks verified working: `sync_turn()`, `on_pre_compress()`, `on_session_end()`

### New Cron Created
- `5aea298eb0a8`: Daily Session Review at 0h (reads yesterday's sessions, updates wiki, reports to Telegram)
- Bug fixed: had to explicitly pass `skills=[]` to avoid attaching `hermes-autoresearch` skill

### Skills
- `github-wiki-backup` updated with wiki repo flatten pattern

## 🧠 Key Learnings

1. **Cron skill override bug**: When creating cron without a skill, must pass `skills=[]` explicitly — otherwise defaults to loading the calling skill
2. **Wiki repo flatten pattern**: GitHub shows `wiki/` wrapper → user wants content at root → `git subtree split --prefix=wiki`, rebase, force push
3. **Memory provider priority**: System loads from `plugins/wiki/` before `plugins/memory/wiki/` — stub was loaded instead of full implementation
4. **Backup architecture**: Wiki (Obsidian plugin → `my-llm-wiki`) separate from Hermes data (cron 3AM → `hermes-backup`)

## ⚠️ Cần xử lý

1. **Wiki folder in my-llm-wiki**: Still some confusion about whether `wiki/` folder itself vs contents should be in repo — flattened but user still saw wrapper
2. **Wiki git at `/Volumes/Storage-1/Hermes/wiki`**: Separate git repo from the parent `Hermes` folder — correct setup
3. **Memory provider stub**: Was fixed this session but happened because of plugin load path priority

## 📊 Stats
- Sessions analyzed: 67 files (May 8, 2026)
- Human sessions: 4 direct interactions
- Wiki commits: 1 (715 files changed, 217989 insertions)
- Crons created: 1 (Daily Session Review 0h)

---
*Generated by Hermes — 2026-05-09 00:00*
