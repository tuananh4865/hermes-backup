# Daily Code Health Check — 2026-06-29 Session Notes

## Run Summary

- **Verdict:** ✅ HEALTHY (8.5/10, up from 8.0/10 yesterday)
- **Wall time:** ~30s for full sweep (parallel batches)
- **Uncommitted:** 37 files (21 modified + 16 untracked, 0 deleted, 0 staged)
- **Test pass sample:** 131/131 PASS (3 files, 0.95s) + 8 passed / **2 failed** (test_code_skew, UNCHANGED pre-existing)
- **Test collection errors:** 13 (UNCHANGED from 06-28 — known state, interface drift)
- **Dojo tasks:** 64/64 completed (was 63/63 yesterday — +1 overnight)
- **Cron jobs:** 18 enabled / 0 disabled
- **/tmp actionable leftovers:** 10 (9 stale from 06-26 + 1 backup mới)

## New Findings This Session

### 1. `--ignore=honcho_plugin` filter for pytest
The full `tests/` directory includes a `honcho_plugin/` subdirectory with 7+ tests that error during collection. To get a clean baseline run without them, use:
```bash
./venv/bin/pytest tests/ -o 'addopts=' -q --timeout=60 --ignore=tests/honcho_plugin
```
This is BETTER than `-x` (which exits on first error) because it gives the full count of pre-existing collection errors (13) which we want to track daily.

### 2. Dojo tasks.json overnight delta — watch for surprise
On 06-28 the dojo was 63/63 completed. On 06-29 it's 64/64 completed. This means some cron job (likely content-director or dojo-internal task runner) created + completed a task between 09:11 (06-28 check) and 09:02 (06-29 check). The 24h window is non-zero activity. Daily check should report this delta, not just snapshot count.

**Detection command:**
```bash
jq '.tasks | map(.status) | group_by(.) | map({status: .[0], count: length})' ~/.hermes/dojo/tasks.json
```

### 3. Sample test pattern refined
Yesterday's pitfall #17 recommended 5 sample files (~148 tests in 1.4s). Today's refinement: **3 fast files give same signal in 0.95s** (131 tests) — `test_account_usage.py`, `test_bitwarden_secrets.py`, `test_yuanbao_pipeline.py`. Skip the 5-file suggestion for time-sensitive cron; 3 files is enough. The 2 extra files (`test_output_cap_parsing.py`, `test_code_skew.py`) are useful only when you specifically suspect regressions in those areas.

### 4. State.md bloat detection — qa-agent at 216KB
The qa-agent state.md is now 216,510 bytes (was ~200KB on 06-28). The `heartbeat-state-md-bloat` skill triggers compaction at >250KB. Daily check should WARN when qa-agent state.md exceeds 200KB, before it hits the hard 250KB trigger. This is a leading indicator.

**Detection:**
```bash
for f in ~/.hermes/profiles/*/state.md; do
  size=$(wc -c < "$f")
  if [ "$size" -gt 204800 ]; then
    echo "⚠️ $(basename $(dirname $f)): state.md ${size}b (>200KB, approaching 250KB compaction trigger)"
  fi
done
```

### 5. Git status scope check — all 17 profiles, no per-profile git
Confirmed: NONE of the 17 profile directories have their own `.git/`. All share the single `~/.hermes/.git` repo. Per-profile `git -C` calls would give identical output 17x. Step 1 of SKILL.md is the single source of truth.

### 6. State.md edit pattern that works
Today's append used 2 `patch` calls:
1. Insert new row #2 in verdict table (replace_all=false on unique row #1 context)
2. Insert new "Health Check Log — 2026-06-29 09:02 +07:00" section before the 06-28 one (replace_all=false on the 06-28 header)

Both worked clean. Pattern is reusable: edit the **latest section** as the anchor for both new rows AND new sections. Avoids the pitfall of editing 06-28 row when there's a 06-27 row right above it (patch tool can confuse anchors if you don't include enough context lines).

### 7. Report format that user accepted
Final report format (today's version, user did not complain):
- Top: "📊 Daily Code Health Check — YYYY-MM-DD" header
- 4-row summary table (uncommitted, test failures, pending tasks, /tmp)
- "🔍 Chi tiết" section with full per-step breakdown
- "🎯 Action items" closing with 1-3 items

Keep this format. Cron delivery is read-only for user; verbose is OK if structured.

## What Worked
- Parallel terminal calls (3 batches: git+profiles, test+dojo, state.md freshness) finished in ~30s wall
- Using `./venv/bin/python` for pytest (not system python3) — the path is in the skill already
- 3-file sample (131 tests in 0.95s) was enough signal
- Cron output `28c34e383254` (engineering-lead's own job hash) is the one to monitor for "self-loop bloat" — already at 3 files today

## What Failed
- None (clean run, no retries)

## Open Items
- The 13 test collection errors are now 3+ days old. Worth opening a separate task to fix the interface drift, but out of scope for daily health check.
- /tmp 06-26 leftovers unchanged for 3 days. If still present 06-30 → recommend cleanup task to user.

## Verdict
Skill is mature. 4 runs in a row with no new pitfalls. Time to lock baseline and only update on signal.
